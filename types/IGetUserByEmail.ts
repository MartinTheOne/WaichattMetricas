export interface UserDB{
  id_cliente: string;
  email: string;
  password: string;
  name: string;
  rol: string;
}
export interface User {
  id: string;
  email: string;
  name: string;
  rol: string;
}